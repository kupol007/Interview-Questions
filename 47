 Explain Identical operator ?
 
 In swift 3 and above

=== (or !==)
Checks if the values are identical (both point to the same memory address).
Comparing reference types.
Like == in Obj-C (pointer equality).
== (or !=)
Checks if the values are the same.
Comparing value types.
Like isEqual: in Obj-C (However, you can override isEqual in Obj-C).




Объясните идентичный оператор?
 
 В swift 3 и выше

=== (или !==)
Проверяет, идентичны ли значения (оба указывают на один и тот же адрес памяти).
Сравнение ссылочных типов.
Как == в Obj-C (равенство указателей).
== (или !=)
Проверяет, совпадают ли значения.
Сравнение типов значений.
Like равен: в Obj-C (однако вы можете переопределить isEqual в Obj-C).
